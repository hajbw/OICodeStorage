#include <iostream>
#include <queue>

using std::cin;
using std::cout;
using std::queue;

int main()
{
	int status,m;
	queue<int> que,layer;

	cin>>status>>m;

	que.push(status);
	layer.push(1);

	while(!que.empty())
	{
		
	}

	return 0;
}
